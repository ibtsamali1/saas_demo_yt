[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
watchPatterns = [
  "requirements.txt",
  "src/**",
  "railway.toml",
  "Dockerfile"
]

[deploy]
startCommand = "cd src && python manage.py migrate && gunicorn demo_saas.wsgi"